{
  "name": "web-scraper-suite",
  "version": "0.1.0",
  "scripts": {
    "dev": "python -m flask run --host=0.0.0.0 --port=5000",
    "build": "vite build"
  },
  "proxy": "http://127.0.0.1:5000",
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
